{
    "version": "0.2.0",
    "configurations": [
        
        {
            "cwd":"${workspaceFolder}/data",
            "name": "Python: Current File",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "justMyCode": true
        }
    ]
}